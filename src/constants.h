#define HEIGHT 1000
#define WIDTH 1000
#define NUM_AGENTS 10000
#define NUM_TASKS 200
#define TOTAL_DEMAND 200
// TODOs:
// pointer to agents, see if that works any better (no need to copy each agent object)
// fix bug
// run better simulations and benchmarks
// figure out propagation stuff, set up a framework